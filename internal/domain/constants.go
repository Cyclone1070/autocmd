package domain

// Provider IDs
const (
	ProviderGoogle = "google"
)

// Auth Method and Field IDs
const (
	AuthMethodAPIKey = "api_key"
	AuthMethodEnv    = "env"
	AuthFieldAPIKey  = "api_key"
)

// UI and Formatting
const (
	ModelIDSeparator = "/"
)
